package ungphys.lund;

public class FSTSorter {
	not = 0;
	n = FSTSorter(FST[]);
	
	for (i < n)
	{
		if item = FST[i]
				{
			FST[i][1]++
				}
		else 
		{
			i ++
			not ++
		}
		
		if not == n
				{
			add item
				}
return FST
	
	}

}
