package ungphys.lund;

//This is a class to collect the Final State Topologies of the LUND File. FSTSorter is the class/function

public class FSTSorter {
	
	//not is the number of topologies and it begins at 0.
	not = 0;
	//Starting at the first index of the array
	n = FSTSorter(FST[]);
	
	//If there is an item to be read from the sorter at the index then add that item and increase the count of that item.
	//In this case topologies.
	for (i < n)
	{
		if item = FST[i]
				{
			FST[i][1]++
				}
		else 
		{
			i ++
			not ++
		}
		
		if not == n
				{
			add item
				}
return FST
	
	}

}
